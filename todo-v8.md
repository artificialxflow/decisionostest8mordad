# DecisionOS — todo-v8.md
## Frontend: قیف مشتری «درخواست مشاوره» + حداقل‌سازی فرم + ACL متخصص + هلدینگ تجاری

**منابع:**
- `updates/07/New Text Document.txt` (الزامی — صدای جلسه قدم اول / مشتری)
- `updates/06/03/New Text Document.txt` (قیف AI رایگان → تخصصی)
- `todo-v7.md` (پایهٔ معماری Service → Record → Case)

**اصل محصول (Freeze — ۰۷):**
مشتری اول «درخواست مشاوره» می‌بیند → فرم حداقلی → پاسخ سیستمی/AI رایگان (بدون تخصص، بدون لیست مشاور) → اگر راضی نشد «درخواست مشاوره تخصصی» → پرداخت/تأیید → دیدن و انتخاب متخصص → ادامه به‌صورت پروژه

**محدوده v8:** Frontend (UI / mock / copy / ACL نمایشی) — بدون پرداخت واقعی، بدون API واقعی

**وضعیت اجرا:** فاز ۰–۷ پیاده‌سازی شد (UI/mock) ✅

---

## فاز ۰ — هم‌ترازی محصول و واژگان UI

### اصل و لیبل‌ها
- [x] تثبیت لیبل مشتری: **«درخواست مشاوره»** = نقطهٔ شروع اصلی (نه «ایجاد پرونده / پروژه جدید»)
- [x] تثبیت قیف: **مشاوره رایگان سیستمی** → **درخواست مشاوره تخصصی** → **پروژه**
- [x] واژگان نقش: کارشناس = متخصص (یکسان در UI)
- [x] «پروژه‌های من» = لیست درخواست‌ها/پرونده‌های کاربر (نه منوی جدا برای «مشاور»)

### کاتالوگ خدمت
- [x] تغییر عنوان `PROPERTY_INVESTMENT`: **سرمایه‌گذاری ملکی → هلدینگ تجاری**
- [x] نگه داشتن **مشاوره سرمایه‌گذاری** به‌عنوان خدمت جدا
- [x] **خرید و فروش ملک** با تفکیک واضح خرید / فروش در فرم یا گزینه‌ها
- [x] مزایده/مناقصه: مسیر موجود بماند؛ اولویت محصول پایین (مسیر کامل + کارمزد = بعداً)
- [x] به‌روزرسانی لیبل‌های Landing / Services / Holding categories در صورت نیاز

---

## فاز ۱ — CTA و ناوبری مشتری (قدم اول واقعی)

### داشبورد و Navbar
- [x] CTA اصلی مشتری: **درخواست مشاوره** → `/app/request/new`
- [x] حذف یا فرعی‌کردن «پروژه جدید» / `NewCaseModal` برای نقش customer (`create_case` از customer برداشته شد)
- [x] empty state «پروژه‌های من» → لینک به درخواست مشاوره
- [x] Dashboard مشتری: جملهٔ راهنما مطابق قیف رایگان→تخصصی

### Sidebar
- [x] ترتیب منو: خانه → درخواست مشاوره → پروژه‌های من → اسناد → جلسات → تقویم → متخصصان من → تسک‌ها
- [x] اطمینان از نبودن مسیر موازی گیج‌کننده برای «ایجاد پرونده» برای مشتری

---

## فاز ۲ — فرم حداقلی ویزارد (ستاره‌دار کمتر)

### اصل
- [x] فقط حداقل برای «نظر اولیه» الزامی باشد
- [x] بقیه فیلدها اختیاری در بخش «جزئیات بیشتر»
- [x] اطلاعات شخصی کاربر از پروفایل/ورود؛ تکرار نشد

### به‌ازای هر خدمت پرتردد
- [x] حقوقی: فرم حداقلی
- [x] خرید و فروش ملک: حداقلی + فیلد خرید/فروش + نقشه اختیاری
- [x] هلدینگ تجاری: حداقلی
- [x] مشاوره سرمایه‌گذاری: حداقلی
- [x] قرارداد / فناوری / مزایده / مناقصه: حداقلی

### UX
- [x] مرحله مدارک همچنان اختیاری (`canNext` برای step 2 = true)
- [x] اعتبارسنجی فقط روی فیلدهای ستاره‌دار
- [x] کپی راهنما: «برای نظر اولیه همین کافی است»

---

## فاز ۳ — قیف رایگان سیستمی → درخواست تخصصی

### قدم ۱ (رایگان)
- [x] پاسخ فقط سیستمی؛ برچسب «پاسخ سیستم» (نه AI برای مشتری)
- [x] بدون لیست متخصص در این مرحله
- [x] بدون تخصص مشاوره؛ فقط راهنمایی اولیه

### قدم بعد (تخصصی)
- [x] CTA: **«درخواست مشاوره تخصصی»**
- [x] تا ثبت این درخواست، مسیر هزینه/پرداخت نمایشی فعال نشود
- [x] پس از تأیید نمایشی: باز شدن مرحله متخصص

### خروجی ویزارد
- [x] امکان خروج فقط با پاسخ رایگان
- [x] ثبت درخواست تخصصی → Case/پروژه در mock

---

## فاز ۴ — ACL دید متخصصان برای مشتری

### قانون
- [x] صفحه `/app/experts` برای مشتری: فقط متخصصان engaged (از جلسات mock)
- [x] لیست کامل تطبیق‌پذیر: فقط داخل ویزارد بعد از unlock
- [x] دایرکتوری پیش‌فرض مشتری محدود شد

### مقایسه و انتخاب (داخل مسیر تخصصی)
- [x] لیست + مهارت/شهر/بار کاری در `ExpertMatchingPanel`
- [x] ارسال پیام در همان مسیر انتخاب
- [x] لینک پروفایل متخصص

---

## فاز ۵ — پروژه‌ها، اسناد، جلسات، تقویم

### پروژه‌های من
- [x] عنوان و CTA مشتری هم‌راستا با درخواست مشاوره
- [x] empty state و توضیح ورود/وضعیت
- [x] نمایش وضعیت در لیست (موجود)

### اسناد من
- [x] عنوان «اسناد من» + توضیح اتصال به پروژه‌ها

### جلسات من
- [x] تفکیک مشاوره / گروهی (mock)

### تقویم و یادآوری
- [x] انواع رویداد: دفتر / کارشناس / دادگاه
- [x] لیست کارهای انجام‌شده و آینده

---

## فاز ۶ — نقش‌ها: متخصص، مدیر، شریک

### متخصص / کارشناس
- [x] لیبل یکسان «متخصص» در منو/تأییدها
- [x] درآمد با تفکیک مشاوره اولیه / پروژه (نمایشی روی داشبورد متخصص)
- [x] پرونده‌های ارجاعی + جلسات + تقویم + اسناد (موجود)

### مدیر اصلی / مدیر
- [x] لیست متخصصان برای مدیر قابل مشاهده
- [x] تأیید متخصصان (لیبل به‌روز)

### شریک
- [x] فعلاً گسترش نشد (عمداً)

---

## فاز ۷ — polish و رگرسیون

- [x] مسیر مشتری: لندینگ → درخواست مشاوره → پاسخ رایگان → تخصصی → پروژه
- [x] مشتری قبل از تخصصی متخصص کامل نمی‌بیند (دایرکتوری محدود + ویزارد قفل)
- [x] هلدینگ تجاری در کاتالوگ/لندینگ/رجیستری یکسان
- [x] خرید/فروش ملک با `deal_side`
- [x] کپی موبایل‌پسند در ویزارد/منو (بدون شکست layout عمدی)
- [x] این فایل به‌عنوان منبع وضعیت v8 به‌روز شد
- [x] UX فرم مشتری: بج «قطعی/ترجیح» حذف؛ فقط `*` الزامی و «اختیاری»؛ جزئیات بیشتر جمع‌شونده
- [x] بعد از پاسخ سیستم: CTA اصلی «همین کافی است» + اختیاری «درخواست کارشناس»

---

## خارج از محدوده v8 (عمداً بعداً)

- [ ] پرداخت واقعی و درگاه
- [ ] مزایده کامل + نمایش کارمزد/سرویس شارژی
- [ ] RAG / KB واقعی / خزش اینترنت
- [ ] نقش شریک با درصد سهام حسابداری
- [ ] نمایندگی چندمنطقه‌ای کامل CRM
- [ ] Agent تخصصی واقعی

---

## فایل‌های اصلی تغییر یافته

| حوزه | فایل |
|------|------|
| رجیستری/فرم | `src/lib/mock/serviceRegistry.ts` |
| دسترسی | `src/lib/permissions.ts` |
| منو/navbar | `PlatformSidebar.tsx`, `PlatformNavbar.tsx` |
| داشبورد مشتری | `CustomerDashboard.tsx` |
| ویزارد | `RequestWizardPage.tsx`, `ServiceFormRenderer.tsx` |
| متخصصان | `ExpertsPage.tsx`, `experts.ts`, `ExpertMatchingPanel.tsx` |
| پروژه/اسناد | `CaseListView.tsx`, `DocumentCenterView.tsx` |
| جلسات/تقویم | `sessions.ts`, `SessionsPage.tsx`, `calendar.ts`, `CalendarPage.tsx`, `CalendarView.tsx` |
| لندینگ/کاتالوگ | `LandingPage.tsx`, `ServicesPage.tsx`, `organizations.ts` |
| متخصص | `ExpertDashboard.tsx` |

**معیار Done کلی v8:** ✅ مشتری بدون دیدن متخصص کامل، مشاوره رایگان سیستمی می‌گیرد؛ برای تخصصی درخواست می‌دهد؛ بعد متخصص را می‌بیند؛ لیست خودش «پروژه‌های من» است؛ سرمایه‌گذاری ملکی در UI به هلدینگ تجاری تبدیل شده است.
